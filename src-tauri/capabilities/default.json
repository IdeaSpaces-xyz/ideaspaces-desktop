{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window. Sidecar execution scoped to the ideaspaces CLI binary only. `args: true` is required: Tauri runs a sidecar with the matched scope entry's args (not the call's), so per-command arg allow-lists all resolve to the first entry. Verbs are passed at the call site instead.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "binaries/ideaspaces",
          "sidecar": true,
          "args": true
        }
      ]
    },
    {
      "identifier": "shell:allow-spawn",
      "allow": [
        {
          "name": "binaries/ideaspaces",
          "sidecar": true,
          "args": true
        }
      ]
    },
    "shell:allow-kill"
  ]
}
